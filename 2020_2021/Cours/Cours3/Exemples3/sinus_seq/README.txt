Comparaison entre le calcul de la fonction sin du systeme et une série de Taylor

Pour compiler:

  Se mettre dans le répertoire qui contient ce fichier
  Taper:

    cmake .
    make

  Si tout s'est bien passé : un fichier executable sin_seq.exe
  est créé dans le répertoire
