<<<<<<< HEAD
void matmult1(const std::vector<double> & A,
              const std::vector<double> & V,
              std::vector<double> & W)
{
  size_t i,j,n = W.size(), m = V.size();
  for (i=0; i<n; i++)
    for (j=0; j<m; j++)
      W[i] += A[i*m + j] * V[j];
}
=======
void matmult1(std::vector<double> & W,
              const std::vector<double> & A,
              const std::vector<double> & V) {
  std::size_t i, j, n = W.size(), m = V.size();
  for (i=0; i<n; i++) {
    for (j=0; j<m; j++)
      W[i] += A[i*m+j] * V[j];
    }
}

>>>>>>> e6a48600856d533ad9304aa045887c928c3778f3
